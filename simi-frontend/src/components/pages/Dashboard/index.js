import React from 'react'
import { TempDefault } from '../../templates';

function Dashboard() {
  const data =
  {
    heading: 'Blank Page',
  }

  return (
    <TempDefault value={data} >
      tesss
    </TempDefault>
  )
}

export default Dashboard;